#include <stdio.h>

/**
 * main - prints the name of the file of the program
 * was complied from, followed by a new line.
 * Return: Always 0.
 */
int main()
{
	printf("%s\n", _FILE_);

	return(0);
}
